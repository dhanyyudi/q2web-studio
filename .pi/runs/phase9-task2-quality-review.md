## Review

CHANGES REQUIRED

- Correct: The plan clarification is now reflected in `docs/superpowers/plans/2026-05-04-phase9-polish-and-gap-closure.md:205-245`. It explicitly keeps the WMS served runtime test as a characterization baseline if already green and says not to replace it with GetFeatureInfo in Task 2. The current test title and assertion in `tests/map-render.spec.ts:2189-2214` match that clarified scope.
- Correct: The new tests are focused and readable. The layer search test uses an accessible label, `tests/map-render.spec.ts:2169`, and the diagnostics test uses a small synthetic non Leaflet ZIP helper, `tests/map-render.spec.ts:102-110` and `tests/map-render.spec.ts:2176-2186`, which is more maintainable than mutating React state directly.
- Correct: Existing helpers are reused for fixture import, ZIP download saving, unzip, and static serving, `tests/map-render.spec.ts:18-21`, `tests/map-render.spec.ts:56-81`, and `tests/map-render.spec.ts:176-207`. Cleanup is handled in `finally` blocks for generated ZIPs, temp directories, browser pages, and local servers.
- Blocker: The WMS served runtime test is not deterministic because it waits for a third party WMS request to finish. In `tests/map-render.spec.ts:2209-2214`, the test records only `requestfinished` events whose URL includes `geoserver/wms`, then waits for at least one finished request. The fixture points to `https://ahocevar.com/geoserver/wms`, so this can fail in offline CI, under DNS issues, or when the external WMS is slow or rejects tile requests, even if the runtime correctly attempted the request. For this characterization, capture `runtimePage.on("request", ...)` instead of `requestfinished`, or otherwise route/mock the WMS endpoint locally. That keeps the test about runtime request emission rather than external network success.
