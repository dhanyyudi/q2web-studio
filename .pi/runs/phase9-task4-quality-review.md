## Review
- Correct: Commit `7438721` changes only the PMTiles served runtime listener from `requestfinished` to `request` in `tests/map-render.spec.ts:2253-2258`. The new assertion now matches the actual behavior being verified, namely that the served runtime initiates a fetch for `sample.pmtiles`, not that the full archive download completes. This is consistent with the neighboring WMS served runtime characterization test, which already uses `request` at `tests/map-render.spec.ts:2218-2223`.
- Correct: The conclusion that this was a harness issue is well-supported by the surrounding evidence in the same spec file. The editor side PMTiles parity test still uses `requestfinished` at `tests/map-render.spec.ts:2115-2123`, where completion is reasonable because it runs inside the editor harness and is meant to observe a completed PMTiles fetch after import. The served runtime regression only affected the exported runtime test block at `tests/map-render.spec.ts:2233-2265`, and the production code was not changed in this commit.
- Correct: Determinism is improved, not weakened. Listening on `runtimePage.on("request")` before `goto()` at `tests/map-render.spec.ts:2251-2257` avoids depending on external timing or full transfer completion of a PMTiles archive, which can be flaky for streamed or range based access patterns. I also verified the targeted test passes locally with `npx playwright test tests/map-render.spec.ts -g "phase 9 exported PMTiles runtime fetches"`.
- Note: There is visible duplication between the WMS and PMTiles served runtime tests at `tests/map-render.spec.ts:2198-2265`, including ZIP export, unzip, static server startup, and request capture setup. A shared helper could reduce repetition later, but this is pre-existing structure and not a blocker for this change.
- Note: The assertion remains intentionally narrow, it proves that the served runtime issues a request containing `sample.pmtiles`, but it does not validate response status, range behavior, or rendered raster output. That is acceptable for the stated harness fix, and I do not see evidence that stronger coverage was promised in this task.

APPROVED
