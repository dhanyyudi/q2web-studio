CHANGES REQUIRED

## Review
- Correct: `src/components/SidePanel.tsx:30-38` filters layers with `useMemo`, and the query matches both `displayName` and layer type. The implementation is small and readable. `tests/map-render.spec.ts:2163-2171` covers the happy path by asserting the filtered list shrinks to one row for `Batas`.
- Correct: `src/components/SidePanel.tsx:132-138` keeps diagnostics rendered in a dedicated panel, and `tests/map-render.spec.ts:2173-2191` verifies the warning text is still visible after switching the layer display mode.
- Blocker: The search state is never reset when the loaded project changes. In `src/components/SidePanel.tsx:30`, `layerQuery` is component local state, but there is no effect tied to `project` changes to clear it. Because the side panel stays mounted across imports, a stale query from the previous project will continue filtering the next project’s layers and can immediately produce the empty state at `src/components/SidePanel.tsx:129` even though the new import succeeded. This is a user visible regression in the import workflow, not just a test concern.
- Note: The diagnostics test relies on mutating `window.__q2ws_project.diagnostics` directly in debug mode, then forcing a rerender indirectly by clicking `Selected layer`, see `tests/map-render.spec.ts:2178-2189`. That is minimal, but it is coupled to App’s debug hook in `src/App.tsx:118-123` and to unrelated rerender behavior. Once the blocker above is fixed, this test may still be worth revisiting later if a more explicit diagnostics injection hook is introduced.